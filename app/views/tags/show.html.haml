.wrapper
  =render partial: "shared/header"

  .side
    =render partial: "shared/side", collection: @tags

  .main
    %h2 Posts(#{@tag.tag})
    =render partial: "tags/post", collection: @posts

    = paginate(@posts)