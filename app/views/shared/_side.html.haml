.side_bar
  .search Search
  .search__form
    = form_tag "/posts/search", method: :get do
      = text_field_tag :search, params[:search]
      = submit_tag "Search", name: :nil

  .genre Genre
  - @tags.each do |tag|
    .genre__link
    = link_to "#{tag.tag}", "/tags/#{tag.id}", class: "genre-btn"